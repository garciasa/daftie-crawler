on:
  push:
    branches:
      - master
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: Build Backend container
        run: cd backend && docker build -t mycontainerhub.azurecr.io/daftiecrawler_backend:latest .
